function state(prop, email){       
    //TODO: 
    //function to set the Stage, per each run!

    if(prop==1){
        createMsg('email', 0, 'msg', 'sam@gmail.com');

    }
    if(prop==2){
        createRoom(email)
    }

}